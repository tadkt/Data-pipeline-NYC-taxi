1. To create a debezium connector, run the run.sh file using the following command in the terminal/console 
    (base) [lam@archlinux snapd]$ run.sh register_connector data_streaming/debezium /debezium.json


    